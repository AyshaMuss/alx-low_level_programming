#include "main.h"

/**
 * print_sign - -Entry point
 *@n: The character to print
 *
 * Return: Always 0.
 */
int print_sign(int r)
{
int r;
r = print_sign(98);
if (r < 0)
r *= -1;
_putchar(r + '0');
_putchar('\n');
return (0);
}
