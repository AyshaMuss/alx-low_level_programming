#include "main.h"
/**
 * print_alphabet -Entry point
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
char c;
int count;
for (count = '0'; count < 10; count++)
{
for (c = 'a'; a <= 'z'; c++)
_putchar(c);
}
_putchar ('\n');
}
