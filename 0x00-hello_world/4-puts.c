#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main()
{
   char string[40];
   strcpy(str, "Programming is like building a multilingual puzzle\n");
   puts(string);
   return 0;
}